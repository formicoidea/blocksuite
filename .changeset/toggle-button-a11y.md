---
'@labre/affine-components': patch
'@labre/affine-block-paragraph': patch
'@labre/affine-block-list': patch
'@labre/affine-block-root': patch
---

feat(blocks): the collapse chevron of headings and lists is a real button (aria-expanded / aria-controls / visible focus), reachable and operable by keyboard; Tab is no longer swallowed by the editor when the focus sits on a native control outside the text
