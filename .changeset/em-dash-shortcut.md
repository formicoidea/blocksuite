---
'@labre/affine-inline-preset': patch
---

feat(blocks): typing `--` followed by a space turns the two hyphens into an em dash (`—`); undo brings them back, and the `---` divider shortcut, `--flag` words and inline code are left alone
